matches:
  # ==============================================================================
  # TEXTOS DO CELULAR
  # ==============================================================================
  # --- Texto padrão ---
  - trigger: ":cel"
    html: |
      {{resposta_celular}}
    vars:
      - name: resposta_celular
        type: script
        params:
          args:
            - python
            - "%CONFIG%\\scripts\\celular.py"
